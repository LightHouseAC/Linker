spring:
  application:
    name: linker-distributed-id-generator
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        group: DEFAULT_GROUP
        namespace: linker
        server-addr: http://127.0.0.1:8848